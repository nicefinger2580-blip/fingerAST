<view class="page login-page">
  <view class="login-bg"></view>

  <view class="login-body">
    <view class="brand-block">
      <view class="brand-logo">📄</view>
      <view class="brand-name">指间论文</view>
      <view class="brand-desc">智能论文创作与模板分享</view>
    </view>

    <view class="card profile-preview-card">
      <view class="preview-label text-faint">设置你的头像与昵称</view>

      <view class="auth-field">
        <text class="field-label">头像</text>
        <view class="field-body">
          <view class="field-fill"></view>
          <view class="avatar-wrap">
            <button
              wx:if="{{!isMiniApp}}"
              class="avatar-picker"
              open-type="chooseAvatar"
              bind:chooseavatar="onChooseAvatar"
            >
              <image
                wx:if="{{previewAvatar}}"
                class="preview-avatar"
                src="{{previewAvatar}}"
                mode="aspectFill"
              />
              <view wx:else class="preview-avatar preview-avatar-empty">点击选择</view>
            </button>
            <view wx:else class="avatar-picker" bindtap="onPickAvatarTap">
              <image
                wx:if="{{previewAvatar}}"
                class="preview-avatar"
                src="{{previewAvatar}}"
                mode="aspectFill"
              />
              <view wx:else class="preview-avatar preview-avatar-empty">从相册选择</view>
            </view>
          </view>
          <text class="text-faint arrow">›</text>
        </view>
      </view>

      <view class="auth-divider"></view>

      <view class="auth-field">
        <text class="field-label">昵称</text>
        <view class="field-body">
          <input
            class="nickname-input field-fill"
            type="text"
            maxlength="32"
            placeholder="请输入昵称"
            value="{{previewNickName}}"
            bindinput="onNicknameInput"
            bindblur="onNicknameBlur"
          />
          <text class="text-faint arrow">›</text>
        </view>
      </view>
    </view>

    <view class="card auth-card">
      <view class="auth-tip text-muted">
        选择头像并填写昵称后即可登录。
      </view>
      <button
        class="wx-auth-btn {{canLogin ? '' : 'wx-auth-btn-disabled'}}"
        bindtap="onSubmitLogin"
        loading="{{loggingIn}}"
        disabled="{{loggingIn}}"
      >
        完成登录
      </button>
    </view>

    <view class="cancel-link" bindtap="onCancel">暂不登录</view>
  </view>
</view>
